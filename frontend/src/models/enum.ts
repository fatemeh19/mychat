enum messageTypes {
    text = 'text',
    image = 'image',
    voice = 'voice',
    video = 'video'

}

enum MessageBoxProps {
    rtl = 'rtl',
    ltr = 'ltr'
}

enum ChatType {
    Private = 'Private',
    Group = 'Group',
    Channel = 'Channel'
}
export { messageTypes, MessageBoxProps, ChatType }